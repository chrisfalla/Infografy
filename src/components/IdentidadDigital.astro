<section class="identidad section" id="identidad">
  <h2 class="section-title">Identidad Digital Responsable y Segura</h2>
  <p class="identidad-intro">Tres acciones clave para mantener una presencia digital profesional:</p>
  <div class="identidad-grid grid grid-3">
    <div class="identidad-card card">
      <div class="identidad-number">01</div>
      <div class="identidad-icon">
        <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor">
          <path d="M18 8h-1V6c0-2.76-2.24-5-5-5S7 3.24 7 6v2H6c-1.1 0-2 .9-2 2v10c0 1.1.9 2 2 2h12c1.1 0 2-.9 2-2V10c0-1.1-.9-2-2-2zm-6 9c-1.1 0-2-.9-2-2s.9-2 2-2 2 .9 2 2-.9 2-2 2zm3.1-9H8.9V6c0-1.71 1.39-3.1 3.1-3.1 1.71 0 3.1 1.39 3.1 3.1v2z"/>
        </svg>
      </div>
      <h3>Proteger Información Personal</h3>
      <p>Mantener datos personales seguros y privados en todas las plataformas digitales.</p>
    </div>

    <div class="identidad-card card">
      <div class="identidad-number">02</div>
      <div class="identidad-icon">
        <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor">
          <path d="M21 3H3c-1.1 0-2 .9-2 2v14c0 1.1.9 2 2 2h18c1.1 0 2-.9 2-2V5c0-1.1-.9-2-2-2zm0 16H3V5h18v14zM5 15h14v3H5z"/>
        </svg>
      </div>
      <h3>Imágenes Profesionales</h3>
      <p>Mantener un contenido visual que refleje profesionalismo y coherencia con mi identidad creativa.</p>
    </div>

    <div class="identidad-card card">
      <div class="identidad-number">03</div>
      <div class="identidad-icon">
        <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor">
          <path d="M12 1L3 5v6c0 5.55 3.84 10.74 9 12 5.16-1.26 9-6.45 9-12V5l-9-4zm0 10.99h7c-.53 4.12-3.28 7.79-7 8.94V12H5V6.3l7-3.11v8.8z"/>
        </svg>
      </div>
      <h3>Cuidado con lo que se Publica</h3>
      <p>Ser consciente y responsable con toda la información que se comparte en internet.</p>
    </div>
  </div>
</section>

<style>
  .identidad {
    background: linear-gradient(135deg, var(--color-bg-cream) 0%, var(--color-bg) 100%);
  }

  .identidad-intro {
    text-align: center;
    font-size: 1.1rem;
    color: var(--color-text-light);
    max-width: 600px;
    margin: -1rem auto 3rem;
  }

  .identidad-grid {
    max-width: 1000px;
    margin: 0 auto;
  }

  .identidad-card {
    text-align: center;
    padding: 2.5rem 2rem;
    position: relative;
    overflow: hidden;
  }

  .identidad-number {
    position: absolute;
    top: 1rem;
    right: 1.5rem;
    font-size: 3rem;
    font-weight: 700;
    color: var(--color-accent-gold);
    opacity: 0.15;
    font-family: var(--font-heading);
  }

  .identidad-icon {
    width: 80px;
    height: 80px;
    background: linear-gradient(135deg, var(--color-accent-gold), var(--color-accent-dark));
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    margin: 0 auto 1.5rem;
    transition: transform 0.3s ease, box-shadow 0.3s ease;
  }

  .identidad-card:hover .identidad-icon {
    transform: scale(1.1);
    box-shadow: 0 8px 25px rgba(212, 165, 116, 0.4);
  }

  .identidad-icon svg {
    width: 40px;
    height: 40px;
    fill: var(--color-white);
  }

  .identidad-card h3 {
    color: var(--color-text);
    margin-bottom: 1rem;
    font-size: 1.2rem;
  }

  .identidad-card p {
    font-size: 0.95rem;
    color: var(--color-text-light);
    line-height: 1.7;
  }

  @media (max-width: 768px) {
    .identidad-grid {
      grid-template-columns: 1fr;
      gap: 1.5rem;
    }

    .identidad-card {
      padding: 2rem 1.5rem;
    }

    .identidad-icon {
      width: 70px;
      height: 70px;
    }

    .identidad-icon svg {
      width: 35px;
      height: 35px;
    }

    .identidad-number {
      font-size: 2.5rem;
    }
  }
</style>
